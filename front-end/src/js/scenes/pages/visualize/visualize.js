import React from 'react';
import 'antd/dist/antd.css';
import {VisualizeContainer} from '../../../containers/visualizeContainer';

const Visualize = () => {
  return (
    <VisualizeContainer></VisualizeContainer>
  );
};

export default Visualize;